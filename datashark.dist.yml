datashark:
  processors: #----------------------------------------------------[PROCESSORS]
    psort:
      bin: /usr/bin/psort.py
    tskape:
      bin: /opt/dfirtk/tskape/tskape
    binwalk:
      bin: /usr/bin/binwalk
    foremost:
      bin: /usr/bin/foremost
    log2timeline:
      bin: /usr/bin/log2timeline.py
